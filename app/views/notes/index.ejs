<html lang="en">
<head>
</head>
<body>

    <main>
        <div>
            <% notesListData.forEach(function(item){
            		if (item.type == "dir") { %>
						<li>
                        	<a href="/notes/<%= item.name %>"><%= item.name %></a>
						</li>
			<%     }
				})%>
        </div>
    </main>

    
</body>
</html>
